import { memo, type FC } from "react";

export const Preview: FC = memo(() => {
    return (
        <div className="w-[230px] box-border border-r border-[#ccc]">
            preview
        </div>
    );
})
